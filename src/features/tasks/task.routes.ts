import { Route } from '@angular/router';
import { TaskListContainer } from './containers/task-list/task-list.container';

export default [{ path: '', component: TaskListContainer }] satisfies Route[];
